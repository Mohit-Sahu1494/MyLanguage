{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run MyLang",
            "type": "shell",
            "command": "${workspaceFolder}\\src\\me.exe",
            "args": [
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}